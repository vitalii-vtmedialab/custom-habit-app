# 4-8 digit PIN for the app gate
APP_PIN=1234

# Vercel Postgres (auto-populated by `vercel env pull` after creating the database)
POSTGRES_URL=

# Vercel Blob (auto-populated by `vercel env pull` after creating the Blob store)
BLOB_READ_WRITE_TOKEN=
